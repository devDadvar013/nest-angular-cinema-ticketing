[TEMPLATE]
# Port the API listens on
PORT=3000

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/cinema

# Comma-separated list of allowed frontend origins (CORS).
# Defaults to local dev origins; set `*` to allow any origin.
# CORS_ORIGINS=http://localhost:4200,https://angular-cinema-ticketing.vercel.app
